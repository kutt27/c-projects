#define LENGTH 30;

#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int random_key_generation(int length){
    srand(time(NULL)); 
}

void printchar(char *text, char *passage){
    printf("%s = '%c' (0x%x)\n", passage, c, c);
}

int main(){
    char cleartext[LENGTH], key[LENGTH];


}